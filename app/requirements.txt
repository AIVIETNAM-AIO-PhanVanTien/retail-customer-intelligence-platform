# Runtime deps for the churn serving Space (kept minimal — no dbt/airflow/mlflow)
streamlit>=1.50.0          # width="stretch" API (use_container_width removed)
pandas>=2.2.0
numpy>=1.26.0,<2.0
pyarrow>=15.0.0
plotly>=5.18.0

# Pinned to the versions the model was pickled with (see model/metadata.json).
# Mismatched majors can break unpickling — keep these aligned with training.
scikit-learn==1.7.2
xgboost>=2.0.0
