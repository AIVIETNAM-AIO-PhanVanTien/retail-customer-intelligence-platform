# ─── Core Data Processing ───
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=15.0.0

# ─── Storage / Lake ───
deltalake>=0.17.0           # Delta Lake read/write (local)

# ─── Analytics Engine ───
duckdb>=0.10.0              # In-process SQL engine for dbt

# ─── Transform ───
dbt-core==1.8.8
dbt-duckdb==1.8.4           # dbt adapter for DuckDB (1.9+ pulls protobuf>=5, breaks mlflow 2.11)
protobuf<5                  # pin: mlflow 2.11 needs protobuf<5; dbt 1.9 would force >=5

# ─── Machine Learning ───
scikit-learn>=1.4.0
xgboost>=2.0.0,<3.0.0
shap>=0.45.0,<0.50            # Feature explainability (numpy<2 compat)
joblib>=1.3.0

# ─── ML Tracking ───
mlflow==2.11.0              # Experiment tracking + model registry

# ─── Orchestration (local dev) ───
# apache-airflow>=2.9.0    # Installed inside Docker container, not locally

# ─── Serving / Dashboard ───
streamlit>=1.32.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
requests>=2.31.0

# ─── Visualization ───
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0

# ─── Testing & Code Quality ───
pytest>=8.0.0
httpx>=0.27.0
ruff>=0.4.0
sqlfluff>=3.0.0

# ─── Environment & Config ───
python-dotenv>=1.0.0
PyYAML>=6.0.1

# ─── Optional: Drift Monitoring ───
# evidently>=0.4.0         # Uncomment when needed (Sprint 4)
